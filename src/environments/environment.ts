export const environment = {
    production: false,
    apiUrl: 'http://localhost:3000'
};
//"build-prod": "ng build --base-href /app-angular/ --output-hashing=all --configuration production && node scripts/generate-hash.js > dist/compilados/version-hash.json"
// npm run build-testing
// npm run build-prod