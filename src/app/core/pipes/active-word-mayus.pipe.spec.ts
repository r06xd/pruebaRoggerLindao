import { ActiveWordMayusPipe } from './active-word-mayus.pipe';

describe('ActiveWordMayusPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveWordMayusPipe();
    expect(pipe).toBeTruthy();
  });
});
